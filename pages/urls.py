from django.urls import path
from . import views

urlpatterns = [
    # path('', views.PageList.as_view(), name="page_list"),
    # path('<path:url>', views.PageDetail.as_view(), name="page_detail"),
]
